import type { GalleryConfig } from "@/types/galleryConfig";

// Add albums here after placing their images under public/gallery/<album-id>/.
export const galleryConfig: GalleryConfig = {
	albums: [],
	columnWidth: 240,
};
